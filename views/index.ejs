<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"
    />
    <link rel="stylesheet" href="/css/styles.css" type="text/css" />
    <title>WatchNext</title>
  </head>
  <body>
    <h1>WatchNext</h1>
    <h2>Choose a Random Movie</h2>
    <ul class="todoItems">
      <li class="item">
        <h4 class="itemText"><%= movies[rand].name %></h4>
        <span class="status"
          ><%= movies[rand].watched ? "Watched" : "Not Watched" %></span
        >
        <span class="fa fa-check">Watch</span>
        <span class="fa fa-trash bin">Delete</span>
      </li>
    </ul>
    <form action="/" method="get">
      <button id="random">Random Movie</button>
    </form>

    <h2>Search for a movie.</h2>
    <form action="/addMovie" method="POST">
      <input type="text" name="movie" placeholder="Enter movie" />
      <input type="submit" value="Submit" />
    </form>
    <div class="container">
      <ul class="todoItems">
        <h3>Not Watched</h3>
        <% for(let i=0; i < movies.length; i++) {%> <% if(!movies[i].watched){%>
        <li class="item">
          <h4 class="itemText"><%= movies[i].name %></h4>
          <span class="status"
            ><%= movies[i].watched ? "Watched" : "Not Watched" %></span
          >
          <span class="fa fa-check">Watch</span>
          <span class="fa fa-trash bin">Delete</span>
        </li>
        <% } %> <% } %>
      </ul>
      <ul class="todoItems">
        <h3>Watched</h3>
        <% for(let i=0; i < movies.length; i++) {%> <% if(movies[i].watched){%>

        <li class="item">
          <h4 class="itemText"><%= movies[i].name %></h4>
          <span class="status"
            ><%= movies[i].watched ? "Watched" : "Not Watched" %></span
          >
          <span class="fa fa-check">Rewatch</span>
          <span class="fa fa-trash bin">Delete</span>
        </li>
        <% } %> <% } %>
      </ul>
    </div>
    <script src="/js/app.js" type="text/javascript"></script>
  </body>
</html>
